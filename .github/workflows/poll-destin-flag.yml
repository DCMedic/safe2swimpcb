name: Poll current Destin beach flag

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/poll-destin-flag.yml'
      - 'scripts/update_destin_current.py'
      - 'scripts/common.py'
  schedule:
    - cron: '27 * * * *'

permissions:
  contents: write

concurrency:
  group: knowthegulf-data-write
  cancel-in-progress: false

jobs:
  poll:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - run: python -m pip install --disable-pip-version-check requests beautifulsoup4
      - name: Validate Destin collector
        run: python -m py_compile scripts/update_destin_current.py
      - name: Refresh official Destin flag
        run: python scripts/update_destin_current.py
      - name: Commit Destin flag heartbeat
        run: |
          if [[ -z "$(git status --porcelain data/destin/current_flag.json)" ]]; then exit 0; fi
          git config user.name "knowthegulf-data-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/destin/current_flag.json
          git commit -m "data: refresh Destin beach flag"
          for attempt in 1 2 3; do
            if git pull --rebase && git push; then exit 0; fi
            if [[ "$attempt" -eq 3 ]]; then echo "Failed to persist Destin flag after 3 attempts." >&2; exit 1; fi
            sleep $((attempt * 5))
          done
